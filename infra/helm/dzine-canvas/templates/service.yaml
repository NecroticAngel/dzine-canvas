apiVersion: v1
kind: Service
metadata:
  name: {{ include "dzine-canvas.fullname" . }}
  labels: {{- include "dzine-canvas.labels" . | nindent 4 }}
spec:
  selector: {{- include "dzine-canvas.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
